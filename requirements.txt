# NavSphere navigation API - runtime + test dependencies.
# Versions match the validated local environment (Python 3.13).
# Railway installs these with `pip install -r requirements.txt`.

# HTTP API
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.30.0,<1.0
pydantic>=2.9.0,<3.0

# Engine / data stack
numpy>=2.0.0
pandas>=2.2.0
scipy>=1.14.0
PyYAML>=6.0.0
pyarrow>=15.0.0

# Learned models (D-T2/D-T3/D-T4/D-T5 artifacts)
scikit-learn>=1.5.0
joblib>=1.4.0
torch>=2.2.0

# Used by committed evaluation scripts (train_baselines.py / evaluate.py);
# not required by the API runtime itself but kept so the scripts stay runnable.
xgboost>=2.0.0

# Test client (httpx is a FastAPI TestClient dependency)
httpx>=0.27.0